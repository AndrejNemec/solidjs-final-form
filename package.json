{
  "name": "solidjs-final-form-repo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build --filter=./packages/*",
    "publish-all": "turbo run publish-package --filter=./packages/*"
  },
  "devDependencies": {
    "prettier": "^3.1.0",
    "turbo": "latest"
  }
}
